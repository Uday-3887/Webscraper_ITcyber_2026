services:
  - type: web
    name: scrapeflow
    runtime: docker
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: PLAYWRIGHT_HEADLESS
        value: true
