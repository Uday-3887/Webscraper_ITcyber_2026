services:
  scraper:
    build: .
    ports:
      - "5000:8000"
    environment:
      PORT: 8000
      SECRET_KEY: change-me
      PLAYWRIGHT_HEADLESS: "true"
    volumes:
      - ./database:/app/database
      - ./exports:/app/exports
      - ./logs:/app/logs
